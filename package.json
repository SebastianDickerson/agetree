{
  "name": "agetree",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Personal sandcastle-style orchestrator: run coding agents in isolated git-worktree lanes.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
